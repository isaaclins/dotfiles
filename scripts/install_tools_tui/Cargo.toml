[package]
name = "install_tools_tui"
version = "0.1.0"
edition = "2024"

[dependencies]
ratatui = { version = "0.26", default-features = false, features = ["crossterm"] }
crossterm = "0.27"
which = "5"
reqwest = { version = "0.11", default-features = false, features = ["blocking", "rustls-tls"] }
